export default function initExemploFetch() {
  /*
  Exemplo Fetch
  const cep = fetch("https://viacep.com.br/ws/87005020/json/");

  cep
    .then((r) => r.json())
    .then((body) => {
      /* Exemplo Fetch
      console.log(body);
      console.log(body.cep);
      console.log(body.logradouro);
      console.log(body.bairro);
      console.log(body.localidade); 
    });
    */
}
